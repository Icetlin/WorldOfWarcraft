"""quest_tts.daemon — пакет с модулями демона.

  wow_memory     — поиск PID WoW, чтение /proc/<pid>/mem
  tts_engine     — piper/espeak + воспроизведение
  quest_tts_daemon — главный цикл
  config         — настройки
"""
