# Зависимости Python для quest_tts_daemon.
# Версии — минимальные, проверенные в момент написания.
psutil>=5.9
piper-tts>=1.2.0
torch>=2.0
omegaconf>=2.0   # требуется silero TTS v4 (читает .yaml конфиг голоса)
ruaccent>=0.4.0  # автопростановка ударений для silero v2/v3 (модель требует '+')
# onnxruntime поставится как зависимость piper-tts.
# Если хочется GPU-ускорения для piper — замените на onnxruntime-gpu>=1.17.
